{
  "name": "wine-cellar-manager",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node server.js",
    "build": "esbuild app.js --bundle --outfile=dist/app.js --format=iife --platform=browser --target=es2020 && cp index.html styles.css dist/",
    "check": "node --check server.js"
  },
  "dependencies": {
    "@netlify/identity": "^0.4.2"
  },
  "devDependencies": {
    "esbuild": "^0.27.4"
  }
}
